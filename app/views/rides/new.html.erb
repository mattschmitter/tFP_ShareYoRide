<div class="main col-xs-10 col-xs-offset-1">
	<h1>Tell us about your ride</h1>
	<%= simple_form_for @ride do |f| %>
		<%= f.input :year %>
		<%= f.input :make %>
		<%= f.input :model %>
		<%= f.input :description %>
		<br />
		<%= f.submit 'share it', :class => "btn" %>
	<% end %>
</div>